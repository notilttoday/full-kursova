{
  "name": "root",
  "private": true,
  "workspaces": [
    "applications/*",
    "common/*"
  ],
  "scripts": {
    "clean": "npx lerna clean -y && rm -rf node_modules"
  },
  "dependencies": {
    "qrcode-svg": "^1.1.0"
  },
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
